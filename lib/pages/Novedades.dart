import 'package:flutter/material.dart';

class Novedades extends StatelessWidget{
  const Novedades ({Key? key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Pagina para ver las mas recientes \nactualizaciones de los usuarios"),
    );
  }

}