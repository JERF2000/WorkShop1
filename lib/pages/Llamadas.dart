import 'package:flutter/material.dart';

class Llamadas extends StatelessWidget{
  const Llamadas ({Key? key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Registro de las últimas llamadas"),
    );
  }

}